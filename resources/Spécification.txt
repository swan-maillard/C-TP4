Comment récupérer les paramètres lors de l'éxecution ?
-> Dans le main 
-> On peut préciser l'url de base en paramètre ou dans un config


    A -> B

    Links multiMap(Target, [Referer, Heure, NB_link])

    A -> [
        B -> 1
        D -> 2
    ]


    A->D
    multimap(A)
        - [B, 12h, 2]
        - [C, 12h, 2]
        - [D, 12h , 1]


main
    -> main()
        -> new LogScanner(...)
        -> LogScanner.parse()
        ->

struct Link
    - referer
    - NB

LogScanner
    - string logFile
    - string dotFile = NULL
    - bool ignoreAssets = False
    - bool timespan = false
    - int beginningTimespan

    - map<string, map<string, int>> linksList;

    -> parse()
    -> displayTopPages()
    -> generateDotFile()

Map(cible, map(ref, nbLinks))