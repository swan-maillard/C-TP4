Comment récupérer les paramètres lors de l'éxecution ?
-> Dans le main 
-> On peut préciser l'url de base en paramètre ou dans un config


    A -> B

    Links multiMap(Target, [Referer, Heure, NB_link])

    A -> [B, 12h, 1]
    A -> [C, 12h, 2]
    B -> [D, 12h, 1]

    A->D
    multimap(A)
        - [B, 12h, 2]
        - [C, 12h, 2]
        - [D, 12h , 1]


main
    -> main()
        -> new LogScanner(...)
        -> LogScanner.parse()
        ->

struct Link
    - referer
    - Heure
    - NB

LogScanner
    - string logFile
    - string dotFile = NULL
    - bool ignoreAssets = False
    - bool timespan = false
    - int beginningTimespan

    - set nodesList;
    - multimap linksList;

    -> parse()
    -> displayTopPages()
    -> generateDotFile()
